interface:
  display_name: "Jujutsu First"
  short_description: "Start and tidy Jujutsu work safely"
  default_prompt: "Use $jujutsu-first to start coding tasks with `jj new`, tidy local stacks, and use `jj` by default."

policy:
  allow_implicit_invocation: true
