# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
# Enable Powerlevel9k instant prompt. Should stay close to the top of ~/.zshrc.
# if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
#   source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
# fi
# ZSH_THEME="powerlevel10k/powerlevel10k"
# [[ ! -f ~/.config/zsh/p10k.zsh ]] || source ~/.config/zsh/p10k.zsh
bindkey -e  # 强制使用 Emacs/Bash 风格绑定
source ~/.config/zsh/alias.zsh
source ~/.config/zsh/export.zsh
source ~/.config/zsh/function.zsh



source ~/.config/zsh/theme.zsh
source ~/.config/zsh/plugins.zsh
source ~/.config/zsh/zstyle.zsh
source ~/.config/zsh/option.zsh





source ~/.config/zsh/eval.zsh
export WORKING_PATH=/home/mx/Code/python/DiffusionDrive/
export NUPLAN_MAP_VERSION="nuplan-maps-v1.0"
export NUPLAN_MAPS_ROOT="${WORKING_PATH}/dataset/maps"
export NAVSIM_EXP_ROOT="${WORKING_PATH}/exp"
export NAVSIM_DEVKIT_ROOT="${WORKING_PATH}"
export OPENSCENE_DATA_ROOT="${WORKING_PATH}/dataset"

export PYTHONPATH=.
export EARTH_DEPLOY_DIR=/opt/stepai/earth/apps/SOC
export PATH=/opt/stepai/earth/apps/SOC/tools:$PATH
