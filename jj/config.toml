#:schema https://jj-vcs.github.io/jj/latest/config-schema.json

[user]
name = "mxcynic"
email = "mx1545814198@gmail.com"

[ui]
pager = "less -FR" # 设置为空字符串以禁用分页器
# pager = "delta" # 设置为空字符串以禁用分页器
# default-command = ["log", "--limit=8"]
bookmark-list-sort-keys = ["committer-date-"]
default-command = "log"
diff-editor = ":builtin"

# [template-aliases]
# 'commit_timestamp(commit)' = 'commit.author().timestamp()'


[remotes.origin]
auto-track-bookmarks = "wxq*"

[fsmonitor]
backend = "watchman"
watchman.register-snapshot-trigger = true

[git]
subprocess = true
